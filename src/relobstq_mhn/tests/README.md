# Tests

`test_relobstq_core.py` is a minimal method-level test suite for the standalone
folder. It intentionally uses synthetic in-memory data rather than project
datasets.

Run from the parent directory of `relobstq_mhn/`:

```bash
python -m pytest relobstq_mhn/tests -q
```
